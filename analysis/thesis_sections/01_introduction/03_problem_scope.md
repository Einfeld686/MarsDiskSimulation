## 3. 本研究の問題設定（何が未解決か）

衝突直後のデブリは非軸対称であり、高離心・高傾斜の軌道要素を含み得る。一方、長期の衛星形成モデルは、時間平均として軸対称で、ほぼ円軌道の薄い円盤を仮定して状態量（例：ロッシュ限界内側円盤質量 (M_{\rm in})）を定義する。このため、数値流体計算（SPH など）の終端から、長期モデルが前提とする準定常円盤へ落ち着くまでの時間帯が残り、ここではこの時間帯を遷移期と呼ぶ。

遷移期では位相混合や衝突散逸により円軌道化・赤道化が進むが、同時に衝突破砕に伴う粒径分布の変化や、高温条件下での昇華・蒸気散逸も重なり得る（例：Hyodo et al., 2017a,b; Ronnet et al., 2016）。したがって、SPH の出力を長期モデルの入力へ写像するには、遷移期を独立の対象として扱い、そこで生じる不可逆過程を入力更新として取り込む枠組みが必要となる。

本章の目的は、遷移期にロッシュ限界内側（内側円盤）から、長期モデルが明示的に含めない経路で失われる固体質量 (\Delta M_{\rm in}) を見積もり、その損失が長期モデルへ渡す (M_{\rm in}) をどの程度修正するかを定式化することである。具体的には、遷移期の損失を (\Delta M_{\rm in}) として評価し、更新後の内側円盤質量を (M_{\rm in}'=M_{\rm in}(t_0)-\Delta M_{\rm in}) の形で与える。

長期モデルでは、内側円盤の物質は「惑星への落下」と「ロッシュ限界外側への拡散（＝外側成長部への供給）」により除去されるものとして質量収支を更新する（Salmon & Canup, 2012; Canup & Salmon, 2018）。一方、表層からの放射圧ブローアウトや昇華・蒸気散逸といった追加の損失は、長期モデル入力の更新手続きに系統的には組み込まれていない（例：Canup & Salmon, 2018）。この差分を遷移期の追加シンクとして扱い、長期モデルへ接続可能な形で定義する点が、本章の位置づけとなる。

以下では、文献を併記した記述は先行研究の結果または前提を要約したものであり、文献のない記述は本研究で置く定義または仮定である。

### 3.1 研究対象と基本仮定

本節では、遷移期の時間範囲、計算領域、鉛直構造の簡約、および不可逆損失と供給過程の取り扱いを定義し、(\Delta M_{\rm in}) を計算するための枠組みを明確化する。とくに、長期モデルと重複しない形で (\dot{M}_{\rm loss}(t)) を定義し、入力・出力の対応を整理する。

対象は、巨大衝突で形成された火星周回デブリ円盤が、衝突直後の非軸対称・高離心・高傾斜な状態から、長期モデルが仮定する「ほぼ軸対称で円軌道に近い円盤」へ移行するまでの遷移期である。衝突直後の円盤は数値流体計算（例：Citron et al., 2015; Hyodo et al., 2017a）が与えるが、長期モデルへ渡すためには、円盤を軸対称な状態量へまとめ直す必要がある（Hyodo et al., 2017b; Canup & Salmon, 2018）。

#### 3.1.1 遷移期の始点 (t_0) と終点 (t_{\rm end})

数値流体計算が出力する「衝突後数十時間」の円盤を初期条件として用い、その時刻を (t=t_0) とおく（Citron et al., 2015; Hyodo et al., 2017a）。遷移期の終端 (t_{\rm end}) までに内側円盤から不可逆に失われた質量を時間積分し、(\Delta M_{\rm in}) を式\ref{eq:delta_min_integral}で定義する。更新後の内側円盤質量は式\ref{eq:min_prime_update}で与える。

\begin{equation}
\label{eq:delta_min_integral}
\Delta M_{\rm in} = \int_{t_0}^{t_{\rm end}} \dot{M}_{\rm loss}(t),dt
\end{equation}

ここで (\dot{M}_{\rm loss}(t)) は遷移期の不可逆損失率である。長期モデルが扱う「惑星への落下」および「ロッシュ限界外への拡散」と二重計上しないため、放射圧ブローアウトや昇華・蒸気散逸など、遷移期に固有の追加損失に限定して定義する。

\begin{equation}
\label{eq:min_prime_update}
M_{\rm in}' = M_{\rm in}(t_0) - \Delta M_{\rm in}
\end{equation}

ここで (M_{\rm in}(t_0)) は SPH 終端時刻における内側円盤質量であり、(M_{\rm in}') を長期モデルへの入力として用いる。

(t_{\rm end}) は、長期モデルが仮定する準定常円盤に近づき、状態量 (M_{\rm in})・(r_d) で記述できるようになった時刻に対応する（Salmon & Canup, 2012; Canup & Salmon, 2018）。ただし、円軌道化・赤道化の速度は円盤の密度や衝突頻度に依存し、遷移期の長さは一意に定まらない（Hyodo et al., 2017b）。このため (t_{\rm end}) を入力として与え、(\Delta M_{\rm in}) の (t_{\rm end}) 依存性を感度として評価する。

#### 3.1.2 対象領域：ロッシュ限界内側の円盤（半径一次元）

計算対象はロッシュ限界内側の円盤に限定する。岩石質物質のロッシュ限界は火星半径の数倍（例：(a_R\approx 2.7R_{\rm Mars})）に位置するとされ、長期モデルでもこの内側領域を連続円盤として扱う（Canup & Salmon, 2018）。本節では円盤を半径方向のリングに分割して追跡する半径一次元モデル（一次元モデル）を用いる。

一次元モデルで直接評価するのは、遷移期に内側円盤から失われる固体成分の量である。外側円盤での集積や、その後の衛星軌道進化は本章では直接扱わず、更新後の (M_{\rm in}') を長期モデルへ渡すための入力として出力する（Salmon & Canup, 2012; Canup & Salmon, 2018）。

半径方向の粘性拡散（リング間の物質移動）は、遷移期が短い場合には主効果でない可能性がある。そこで標準設定では粘性拡散を含めず、必要に応じて感度解析として追加する。遷移期の更新に含める項と、長期モデルに委ねる項の切り分けは、次章で先行研究の写像手順として整理する。

#### 3.1.3 鉛直方向の簡約：深部と表層

放射圧や強い照射は円盤全体に一様に作用するのではなく、主として表面付近に作用する。光学的に厚い円盤であっても、表面の薄い層は直接照射され、その粒子運動が放射圧の影響を受け得る（Takeuchi & Lin, 2003）。一方、火星デブリ円盤を扱う研究では、視線方向に積分した光学的厚み (\tau) に基づき遮蔽が議論されている（Hyodo et al., 2018）。これらを同一の枠組みで扱うため、鉛直方向を深部と表層に分けて定義する。

各半径リングのうち、照射が届きにくい領域を深部（貯蔵庫）とし、照射の影響を受ける領域を表層（照射される層）とみなす。表層は厳密には一意に定義されないため、本節では鉛直方向の光学的厚みが概ね (\tau\sim1) となる近傍を目安として扱う。

表層の幾何学的な厚みは文献で統一的に与えられていない。そこで衝突頻度の見積もりに用いるスケールハイトとして、幾何学的な近似 (H\simeq i,a) を導入し、厚みの代理指標とする。ここで (i) は代表的な軌道傾斜角であり、表層再供給や衝突頻度を通じて損失量にも影響するため、固定値を仮定せず感度パラメータとして扱う。

数値実験では代表値として (i_0=0.05) および (0.10,\rm rad) を用い、損失量の依存性を比較する。以上の二層化により、損失量が「表層に存在する物質量」と「深部から表層へ供給される速さ」に強く依存する点を明示できる。

表層質量の見積もりと (\tau) の定義の違いが示す物理的含意は、次章で文献に沿って数式として整理する。

#### 3.1.4 表層で結合して扱う過程：破砕・昇華・放射圧

遷移期に内側円盤から不可逆に失われる固体成分は、衝突破砕、放射圧除去、昇華が連鎖して生じ得る。まず衝突による破砕により粒径分布が小粒側へ拡張し、放射圧に敏感な微粒子が供給される。次に微粒子は放射圧を受けやすく、円盤から除去される（Hyodo et al., 2018）。さらに高温条件では昇華により粒径が縮小し、放射圧による除去が促進される。

粒子数密度の時間発展は衝突方程式（Smoluchowski equation）で記述し、サイズビンに離散化して数値的に解く。加えて、昇華によるサイズ変化と放射圧による除去（ブローアウト）判定を同一の時間積分に組み込み、表層からの損失率 (\dot{M}_{\rm loss}(t)) を評価する。

このように過程を結合して扱う狙いは、衝突で供給された微粒子が昇華でさらに小さくなり、放射圧で除去されるという連続過程を途中で切らずに評価する点にある。結果として、粒径分布の時間発展と質量損失を同じモデル内で整合的に扱える。

#### 3.1.5 表層への供給（深部→表層）のパラメータ化

表層からの損失量は、深部がどの程度の速さで表層を補給できるかに強く依存する。原始惑星系円盤では、乱流拡散が深部から表層への供給を担い得ることが議論されている（Takeuchi & Lin, 2003）。しかし火星デブリ円盤ではガス量が不確かであり、ガスの鉛直拡散を既定として置くことは難しい。

そこで深部から表層への供給は、円盤の厚み（軌道傾斜角のばらつき）や衝突による鉛直混合をまとめた効果として「表層再供給」としてパラメータ化する。具体的には、深部と表層の間の質量交換を表す時間スケール (t_{\rm mix})（または効率 (\epsilon_{\rm mix})）を導入し、(t_{\rm end}) と並ぶ主要な入力として扱う。これにより、表層が短時間で枯渇する場合と、深部が表層を継続的に補給する場合を同一の枠組みで比較できる。

#### 3.1.6 入力・出力と、次章への接続

入力は、数値流体計算が与える初期条件と、長期モデルへ接続するために必要な系のパラメータから構成する（Citron et al., 2015; Hyodo et al., 2017a; Salmon & Canup, 2012; Canup & Salmon, 2018）。初期条件としては、内側円盤質量 (M_{\rm in}(t_0)) とその半径方向分布、および表層・深部の配分を含む初期粒径分布を与える。加えて、火星からの照射条件（火星表面温度履歴 (T_{\rm Mars}(t)) など）、表層再供給パラメータ (t_{\rm mix})（または (\epsilon_{\rm mix})）、ならびに遷移期の終端 (t_{\rm end}) を入力として与える。

出力は、遷移期の不可逆損失 (\Delta M_{\rm in}) と、更新後の内側円盤質量 (M_{\rm in}') である。これを長期形成モデルへ渡すことで、従来の「惑星への落下＋粘性拡散」に閉じた質量収支（Salmon & Canup, 2012; Canup & Salmon, 2018）に対し、遷移期の追加シンクを手続きとして接続できる。

以上により、遷移期の物理過程を長期モデルの入力更新として表現する準備が整う。次章では、衝突直後計算の出力を長期モデルの入力へ写像する既存の方法と、その写像が曖昧になりやすい遷移期の扱いを文献に沿って整理する。

<!-- TEX_EXCLUDE_START -->

### 先行研究リンク

* [Takeuchi & Lin (2003)](../../paper/references/TakeuchiLin2003_ApJ593_524.pdf)
* [Salmon & Canup (2012)](../../paper/references/SalmonCanup2012_ApJ760_83.pdf)
* [Citron et al. (2015)](../../paper/references/Citron2015_Icarus252_334.pdf)
* [Ronnet et al. (2016)](../../paper/references/Ronnet2016_ApJ828_109.pdf)
* [Hyodo et al. (2017a)](../../paper/references/Hyodo2017a_ApJ845_125.pdf)
* [Hyodo et al. (2017b)](../../paper/references/Hyodo2017b_ApJ851_122.pdf)
* [Canup & Salmon (2018)](../../paper/references/CanupSalmon2018_SciAdv4_eaar6887.pdf)
* [Hyodo et al. (2018)](../../paper/references/Hyodo2018_ApJ860_150.pdf)

<!-- TEX_EXCLUDE_END -->
