repos:
  - repo: local
    hooks:
      - id: readme-sync-check
        name: README sync check
        entry: python tools/readme_sync.py --check
        language: python
        files: ^(README\.md|analysis/.*\.md)$
        pass_filenames: false
        always_run: true
