[pytest]
testpaths = tests
python_files = test_*.py
markers =
    unit: unit tests
    integration: integration tests
    research: research-only tests (may require extra data)
    legacy: legacy compatibility tests
    slow: slow tests (opt-in)
filterwarnings =
    error
